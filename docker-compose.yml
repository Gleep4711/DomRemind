version: '3.8'
services:
    dom_remind_bot:
        build: .
        container_name: dom_remind_bot

        restart: on-failure:5
        environment:
            - BOT_TOKEN
            - DB_URL
        depends_on:
            - db
        volumes:
            - ./:/app
volumes:
    postgres: