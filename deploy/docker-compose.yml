services:
    dom_remind_bot:
        build: ..
        container_name: dom_remind_bot

        restart: unless-stopped
        environment:
            BOT_TOKEN: ${BOT_TOKEN}
            DB_URL: ${DB_URL}
            ADMIN: ${ADMIN}

            WHOISJSON_API_KEY: ${WHOISJSON_API_KEY}

            LOGGING: info

        networks:
            - main

networks:
    main:
        external: true
